<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE log4j:configuration SYSTEM "log4j.dtd">

<log4j:configuration xmlns:log4j="http://jakarta.apache.org/log4j/" debug="false">

    <!-- A time/date based console appender, priting to stderr -->
    <appender name="CONSOLE" class="org.apache.log4j.ConsoleAppender">
        <param name="target" value="System.err" />
        <param name="Threshold" value="DEBUG"/>
        <layout class="org.apache.log4j.PatternLayout">
            <param name="ConversionPattern" value="%-5p [%t] [%d] [%c] %m%n"/>
        </layout>
    </appender>

    <!-- The full log appender uses a extremely slow pattern, on production
         systems use only this appender with a threshold of fatal.
          Priting to stderr -->
    <appender name="FULL_LOG" class="org.apache.log4j.ConsoleAppender">
        <param name="target" value="System.err" />
        <param name="Threshold" value="DEBUG"/>
        <layout class="org.apache.log4j.PatternLayout">
            <param name="ConversionPattern" value="%-5p [%t] [%d] [%c] - %l [LINENUMBER: %L] [METHOD: %M] [%X]  %m%n"/>
        </layout>
    </appender>


    <logger name="dk.statsbiblioteket.util">
        <level value="WARN" />
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- all loggeres inherit the root logger - so don't include the
         root appenders in the other loggers (will duplicate log entries
         in the log file) -->
    <root>
        <level value="FATAL" />
        <appender-ref ref="FULL_LOG" />
    </root>

</log4j:configuration>
